  rollback:
    needs: healthcheck
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Terraform Rollback
        working-directory: infra/terraform
        run: terraform apply -auto-approve -var="rollback=true"
